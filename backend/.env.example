PORT=4000
ROOT_DIRECTORY=/srv/file-browser/data
# Comma-separated list of origins allowed by CORS in production.
# Leave empty when serving the frontend from the same origin.
# CORS_ORIGIN=https://files.example.com
